{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container mt-4">

        <div class="jumbotron">
            <h1 class="display-4">Bienvenue sur Symrecipe</h1>
            <p class="lead text-justify" >Symrecipe est une application qui va te permettre de créer des recettes à base d'ingrédients
                que tu auras toi-même créé. Tu pourras partager tes recettes à la communauté du site ou bien les garder en privé.</p>
            <hr class="my-4">
            <p>Pour commencer, rendez-vous sur la page d'inscription pour utiliser l'application.</p>
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ path('app_registrationSecurity') }}" role="button">Inscription</a>
            </p>
        </div>

        <div class="recipes">
            <h1>Dernières recettes partagées</h1>
            <div class="d-flex justify-content-between flex-wrap">
                {% for recipe in recipes %}
                <div class="card border-primary m-3" style="max-width: 20rem;">
                    <div class="card-header">Recette n° {{ recipe.id }}</div>
                    <div class="card-body">
                    <h4 class="card-title">
                        <a href="{{ path('app_showRecipe', {id: recipe.id}) }}">{{ recipe.name }}</a>
                    </h4>
                    <p class="card-text">{{ recipe.description|slice(0, 100) ~ " ..." }}</p>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>

    </div>
{% endblock %}
